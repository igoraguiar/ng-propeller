import { PmdDropdownDirective } from './pmd-dropdown.directive';

describe('PmdDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new PmdDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
