import { PmdCheckboxDirective } from './pmd-checkbox.directive';

describe('PmdCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new PmdCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
