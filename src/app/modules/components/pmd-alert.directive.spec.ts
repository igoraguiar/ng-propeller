import { PmdAlertDirective } from './pmd-alert.directive';

describe('PmdAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new PmdAlertDirective();
    expect(directive).toBeTruthy();
  });
});
