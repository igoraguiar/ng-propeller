import { PmdRippleEffectDirective } from './pmd-ripple-effect.directive';

describe('PmdRippleEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new PmdRippleEffectDirective();
    expect(directive).toBeTruthy();
  });
});
